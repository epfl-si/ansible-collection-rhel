---

- name: Converge
  hosts: s3cmd
  roles:

    - role: s3cmd
      s3cmd_user: root
      s3cmd_bucket: bucket1
      s3cmd_default: true

    - role: s3cmd
      s3cmd_user: root
      s3cmd_bucket: bucket2
      s3cmd_read_only: true
