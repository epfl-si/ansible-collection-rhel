---
# tasks file for user

- name: "Add {{ user }} user"
  become: true
  user:
    name: "{{ user }}"
    state: present
    shell: "{{ shell | default('/bin/bash') }}"
    home: "{{ home | default(omit) }}"
    uid: "{{ uid | default(omit) }}"
