---

users:
  hosts:
    user_el8_path:
  vars:

    user_zsh_loop:
      user_name: user_zsh_loop
      user_shell: /bin/zsh
      user_path_add: ['/add_path_zsh']

    user_bash_loop:
      user_name: user_bash_loop
      user_shell: /bin/bash
      user_path: "/usr/bin:/path_bash"

    users_to_manage:
      - "{{ user_zsh_loop }}"
      - "{{ user_bash_loop }}"