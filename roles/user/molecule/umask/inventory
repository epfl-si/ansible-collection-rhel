---

users:
  hosts:
    user_el8_umask:
  vars:

    user_loop:
      user_name: user_loop

    user_loop_umask:
      user_name: user_loop_umask
      user_umask: '0066'

    users_to_manage:
      - "{{ user_loop }}"
      - "{{ user_loop_umask }}"