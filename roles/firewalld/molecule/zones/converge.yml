---

- name: Converge
  hosts: zones
  vars:
    firewalld_zone_sources:
      external:
        source:
          - "{{ hostvars['zone3']['ansible_local']['molecule']['podman_network']['node_ip'] }}"
  roles:

    - firewalld
