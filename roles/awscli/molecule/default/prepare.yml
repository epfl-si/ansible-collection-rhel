---

- name: Prepare
  hosts: awscli
  tasks:

    - name: Install unzip
      ansible.builtin.package:
        name: unzip
        state: present

    - name: Create user john
      ansible.builtin.user:
        name: john
        state: present
