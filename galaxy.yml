---
namespace: epfl_si
name: rhel
version: 2.2.2
readme: README.md

authors:
  - Laurent Indermühle <laurent.indermuehle@epfl.ch>

description: Ansible collection to provision and manage RHEL servers

license:
  - MIT

tags:
  - collection
  - system
  - linux
  - rhel

# Collections that this collection requires to be installed for it to be
# usable. The key of the dict is the
# collection label 'namespace.name'. The value is a version range
# L(specifiers,https://python-semanticversion.readthedocs.io/en/
# latest/#requirement-specification). Multiple version
# range specifiers can be set and are separated by ','
dependencies: {}

# The URL of the originating SCM repository
repository: https://github.com/epfl-si/ansible-collection-rhel

# The URL to any online docs
# documentation: http://docs.example.com

# The URL to the homepage of the collection/project
# homepage: http://example.com

# The URL to the collection issue tracker
issues: https://github.com/epfl-si/ansible-collection-rhel/issues

# A list of file glob-like patterns used to filter any files or directories
# that should not be included in the build artifact. A pattern is matched from
# the relative path of the file or directory of the collection directory. This
# uses 'fnmatch' to match the files or directories. Some directories and files
# like 'galaxy.yml', '*.pyc', '*.retry' and '.git' are always filtered
build_ignore:
  - sftp-config.json
